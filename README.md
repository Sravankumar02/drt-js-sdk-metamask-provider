# drt-js-sdk-metamask-provider
Signing provider for dApps: Metamask Snap

# Installation

The library can be installed via npm or yarn.

```bash
npm install @sravankumar02/sdk-metamask-provider
```

or

```bash
yarn add @sravankumar02/sdk-metamask-provider
```

